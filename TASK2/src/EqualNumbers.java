
public class EqualNumbers {
    public static String compareTwoNumbers(int num1, int num2) {
        String str;
        str = (num1 == num2) ? "equal" : "not equal";
        
        return str;
    }
}
